package $_13_Package;

import java.util.Scanner;

public class Dog {
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);
        Cat cat=new Cat();

        /**
         * we can create dog class object inside cat class because they are
         * in same package and we don't need to import them
         */
    }
}
